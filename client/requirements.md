## Packages
framer-motion | For smooth transitions and game phase animations
lucide-react | For role icons and UI elements
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
WebSocket connects to /ws
Authentication is session-based via sessionId stored in localStorage
Game state updates are pushed via WebSocket
